import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.healthmonitoring.system",
  appName: "Health Monitoring System",
  webDir: "dist",
};

export default config;
